query LaunchList {
    launches {
        cursor
        hasMore
        launches {
            id
            site
            mission {
                missionPatch(size: SMALL)
                name
            }
        }
    }
}